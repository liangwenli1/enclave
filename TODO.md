# TODO

两项已确认要做、但工作量大、暂不动代码的功能。规格来自 2026-09-20 的调研结论。
开工前各自再出一版实现方案；做完之前，官网和套餐表里**不出现**这两项。

---

## 1. 窗口同步（群控）

在一个主窗口里操作，其余选中的环境窗口跟着做。

### 技术路线：系统层 Hook，不走 CDP

主流产品（AdsPower、比特浏览器）都在客户端用 Windows API：`SetWindowsHookEx` 捕获主窗口的
物理输入，按窗口句柄用 `PostMessage` / `SendInput` 广播给从窗口。我们也走这条路，放在
`crates/host`（Rust，`windows` crate），不用 CDP 的 `Input.dispatch*`：

- 往主窗口页面里注入捕获脚本，页面自己就能检测到；系统层 Hook 页面看不到。
- 跨域 iframe、浏览器自身 UI（地址栏、扩展弹窗）CDP 注入脚本够不着，句柄级派发都能覆盖。
- 代价：这是 Windows 专属实现，macOS 要另写一套（`CGEventTap`），排在 mac 版之后。

### 只做「点击、键盘、滚动」不够 —— 以下五项是刚需

| 项 | 要求 | 为什么 |
|---|---|---|
| 坐标随机偏移 | 从窗口的落点在主窗口坐标上加 ±2–5 px 随机量 | 多个号点在同一像素是关联特征 |
| 阶梯随机延迟 | 每个从窗口 20–200 ms 的随机延迟后再执行，窗口之间错开 | 10 个窗口同一毫秒发同样的请求会被判机刷并关联封号 |
| 差异化文本输入 | 「文本分配」：从剪贴板逐行分给各窗口，或按变量模板替换；不是照抄主窗口的字符 | 批量注册/填表时每个号的账号、密码、助记词都不同 |
| 拖拽与轨迹同步 | 同步 `mouseMove` 轨迹和按下/抬起，不只是 click | 滑块验证码（Geetest / hCaptcha / Arkose）全靠拖拽，没有它登录阶段就废了 |
| 一键平铺 | 按宫格把选中窗口排成同样大小 | 坐标按比例换算的前提是布局一致；窗口大小各异时响应式页面元素位置对不上，点击必错位 |

### 拆分（每条一个纵向切片）

1. Host：枚举环境窗口句柄（按 pid → 顶层窗口），`/v1/windows/tile` 一键平铺。**先做这个**，
   它独立有用，也是后面所有坐标换算的前提。
2. Host：主窗口低级鼠标/键盘 Hook → 事件队列 → 对每个从窗口：比例换算 + 抖动 + 延迟 → 派发。
   含 `mouseMove` 与按键状态，拖拽自然成立。
3. 文本分配：工作台里贴多行文本 / 变量表，Host 在「下一次输入焦点」时按窗口序号分发。
4. 工作台 UI：选主窗口、勾从窗口、开始/停止、抖动与延迟范围两个滑杆。热键急停（必须有）。
5. 档位：`syncWindows` 回到许可证载荷，Pro / Team 开放。

### 待定

- 从窗口最小化或被遮挡时 `PostMessage` 仍可达，`SendInput` 不行 —— 优先 `PostMessage`，
  对不吃消息的控件再回落。需要在 fingerprint-chromium 上实测哪些输入必须走 `SendInput`。
- 主窗口里输入密码时是否广播：默认广播，但给一个「暂停同步」热键。

---

## 2. 团队席位与云端同步

Team 账号的所有者邀请成员；成员用自己的账号登录，按角色使用团队的环境。

### 同步什么：元数据 + 轻量会话，不传整个用户目录

不要把几个 GB 的 Chromium 用户目录搬上云。每个环境控制在 **5 MB 以内**：

- **配置（数据库）**：指纹参数 JSON、代理信息。
- **会话资产（对象存储，OSS / S3）**：只打包 `Default/Network/Cookies`、`Default/Local Storage`、
  `IndexedDB` 这些登录态文件，压缩后通常几百 KB 到几 MB。
- **明确排除**：`GPUCache`、`Cache_Data`、`Service Worker` 等临时文件。

### 环境互斥锁

同一时间只允许一个成员打开某个环境。A 打开时云端打 `is_locked`；B 再打开会看到
「该环境正在被 A 运行中」。关闭时客户端把增量 Cookie / Storage 打包回传并释放锁。
这同时解决两件事：数据不会互相覆盖；同一账号不会在两个地域并发登录触发平台风控。

要处理的边界：客户端崩溃或断网没释放锁 → 锁带租约（心跳续期，超时自动释放），
所有者可以强制解锁；强制解锁后原持有者回传的会话要拒收。

### 权限（RBAC）

- 角色：Admin / Operator。
- **隐藏敏感信息**：Operator 只能启动环境，看不到代理密码明文，不能导出 Cookie、不能导出环境包
  —— 防止员工带走店铺资产。
- 这意味着代理密码要由服务端下发给 Host、不经过工作台界面；现在的「保险箱在 WebView 里」
  的模型对 Operator 不成立，需要把解密放到 Host 一侧。

### 前置改造（不做这些，上面都做不了）

1. **环境定义搬出 WebView 的 localStorage，进 Host 的 sqlite。** 现在环境只存在各人浏览器存储里，
   没有可同步的「真相」。这一步单独就值得做（原交付文档 §4 本来就要求 `app.sqlite`）。
2. 会话资产在**客户端加密**后再上传（团队密钥，所有者生成，邀请时用成员公钥包一层）。
   Cookie 是客户最值钱的东西，厂商不应该有能力读到。
3. 许可证服务加：`teams`、`members`（角色）、`invites`、`env_locks`（租约）、对象存储签名 URL。

### 拆分

1. Host sqlite 存环境（前置 1）—— 零功能变化的迁移，含从 localStorage 的一次性导入。
2. 许可证服务：团队、邀请、角色；工作台：成员管理页。此时成员环境仍各自隔离。
3. 环境元数据上云 + 互斥锁（租约）。
4. 会话资产打包、加密、回传。
5. Operator 限制：隐藏代理密码、禁导出。

### 待定

- 对象存储放哪（阿里云 OSS / Cloudflare R2）、谁付流量。
- 一个环境的会话资产超过 5 MB 时：拒绝同步并提示，还是分块。
- 成员离开团队后，他本机上已经解密过的会话副本无法收回 —— 只能在文档里说清楚，
  并在移除成员时轮换团队密钥。
