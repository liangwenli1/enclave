# Enclave 工作台设计规范

这一份管工作台。官网有自己的规范（`enclave-www` 仓库的 `DESIGN.md`）：官网是对外的产品页，工作台是高密度的工具，
版式、间距、组件形状各按各的。**两边共用的是品牌层**：标志（指纹章，种子 12345）、钴蓝 `#1d3cff`、
海军蓝的字 `#0b1b33`、偏蓝的中性色，以及 Instrument Sans + JetBrains Mono 两套字体。改品牌层要两边一起改。

## 1. 一句话

海军蓝的字、偏蓝的中性色、一种钴蓝。分隔靠线，不靠卡片；蓝色只在两处出现；每个环境有一枚由指纹种子算出来的"指纹章"。

不用全大写小标签，不用"·"拼接元信息，不把等宽字体当装饰，不把所有东西塞进一样的圆角卡片。

## 2. 颜色

亮色和暗色都做，默认跟随系统，用户可以在设置里选定（写到 `<html data-theme>`）。

| 角色 | 亮色 | 暗色 | 用在哪 |
|---|---|---|---|
| canvas | `#ffffff` | `#070b14` | 页面和面板的底 |
| ink | `#0b1b33` | `#eef2fb` | 标题、正文里要强调的字。海军蓝，不用纯黑 |
| muted / subtle / faint | `#334560` `#5f6f86` `#9aa8bc` | `#c3cddd` `#8a98ae` `#56647a` | 正文、次要说明、占位 |
| line / line-strong | `#e1e7f0` `#c9d3e2` | `#1c2638` `#2d3a52` | 分隔线、输入框和次按钮的边 |
| surface-2 / surface-3 | `#eff3f9` `#e3e9f2` | `#111a28` `#1a2436` | 悬停、代码块、只读输入 |
| accent | `#1d3cff` | `#3553ff` | 主按钮的底 |
| accent-text | `#1d3cff` | `#8ea3ff` | 蓝色的字和图形（暗色下要更亮才读得清） |
| accent-wash | `#e8eeff` | `#101b40` | 正在运行的行、选中的行、当前页 |
| warn / bad | `#9a5b00` `#c42b1c` | `#f0a53a` `#ff6b6b` | 需要注意、出错 |

**蓝色只有两个用处**：

1. 这一屏要你点的那一个主按钮（一屏一个）。
2. "正在运行 / 已选中 / 没问题"。

出错用红，不占用蓝。不引入绿色。

## 3. 字体

- 西文和数字：**Instrument Sans**（OFL，woff2 打进安装包，不联网加载；400–700）。
- 中文：系统字体（苹方 / 微软雅黑 / Noto Sans CJK）。
- 等宽：**JetBrains Mono**（OFL，同样打进安装包），**只给机器值**：哈希、IP、端口、种子、路径。不用来做小标签。
- 标题靠字重（700）和紧字距（-0.03em），不靠装饰。页面标题 28px，面板标题 16px，正文 14px，说明 13px。
- 数字对齐的地方用 `tabular-nums`。

## 4. 形状与结构

- 圆角只有两档：控件 3px，容器 4px。状态点是圆的，**出错的点是方的**（不只靠颜色区分）。
- 面板 = 一圈 1px 的线，底色和页面一样。不用阴影分层；只有对话框和下拉菜单有阴影，并且用 ink 色描边。
- 表格：表头是普通大小写的 13px 灰字，行与行之间一条线。正在运行的行铺 accent-wash。
- 左侧导航的当前项是浅蓝底、蓝字、左边一道 2px 的蓝线，和"已选中"是同一种蓝，只用来回答"在哪一页"。
- 对话框用 line-strong 描边、海军蓝色调的阴影，遮罩是半透明的深海军蓝。
- 所有页面用同一个 1280px 的外框，标题位置不随导航跳动；表单类内容在框内限宽、靠左。

## 5. 指纹章

`EnvGlyph`：用环境的指纹种子做随机源，画 5 圈断开的同心弧。种子不变图形不变，
复制出来的环境种子不同、章也不同。出现在环境列表、环境详情页标题。
这是整套界面里唯一的装饰，也是只有指纹浏览器才会有的东西。

标志是同一个算法（种子 12345，画 4 圈，`BrandMark`）：侧栏、登录页、应用图标、官网都是它。
应用图标是钴蓝圆角方块上的白色指纹圈；16–64px 的小图标只画 3 圈、线更粗，缩小了也认得出。

## 6. 文案

- 界面不是文档：常驻文字只留出错原因 + 下一步、需要用户同意的后果、一行输入提示。
- 按钮说清楚点了会发生什么（"新建环境""下载""删除"），同一个动作全程同一个名字。
- 用户看不懂的内部词不出现（状态码只在出错详情里以等宽小字出现）。
- 元信息用中文标点连接（"4 个版本，1 个已下载"），不用"·"。
- 禁用的按钮必须在 `title` 里说明原因。

## 7. 动效

只回应用户的动作（展开、切换、确认）。没有进场动画。尊重 `prefers-reduced-motion`。
