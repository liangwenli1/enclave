# Enclave 设计规范

工作台和官网共用这一份。官网仓库（`enclave-www`）里的 `DESIGN.md` 只是指到这里。

## 1. 一句话

纯黑、纯白、一种蓝。分隔靠线，不靠卡片；蓝色只在两处出现；每个环境有一枚由指纹种子算出来的"指纹章"。

方向以 awesome-design-md 里 Vercel 那份为底（黑白加蓝链接色、紧字距的无衬线标题），
再按 frontend-design 的要求去掉模板感：不用全大写小标签，不用"·"拼接元信息，
不把等宽字体当装饰，不把所有东西塞进一样的圆角卡片。

## 2. 颜色

亮色和暗色都做，默认跟随系统，用户可以在设置里选定（写到 `<html data-theme>`）。

| 角色 | 亮色 | 暗色 | 用在哪 |
|---|---|---|---|
| canvas | `#ffffff` | `#000000` | 页面和面板的底。是纯白和纯黑，不是"接近" |
| ink | `#000000` | `#ffffff` | 标题、正文里要强调的字、当前导航项的底 |
| muted / subtle / faint | `#3a3a3a` `#6e6e6e` `#a6a6a6` | `#cfcfcf` `#8c8c8c` `#5c5c5c` | 正文、次要说明、占位 |
| line / line-strong | `#e4e4e4` `#c4c4c4` | `#262626` `#3d3d3d` | 分隔线、输入框和次按钮的边 |
| surface-2 / surface-3 | `#f4f4f4` `#e9e9e9` | `#121212` `#1e1e1e` | 悬停、代码块、只读输入 |
| accent | `#0a3cff` | `#1f4dff` | 主按钮的底 |
| accent-text | `#0a3cff` | `#7d9bff` | 蓝色的字和图形（暗色下要更亮才读得清） |
| accent-wash | `#e8edff` | `#0c1640` | 正在运行的行、选中的行 |
| warn / bad | `#9a5200` `#c40000` | `#f0a53a` `#ff6b6b` | 需要注意、出错 |

**蓝色只有两个用处**：

1. 这一屏要你点的那一个主按钮（一屏一个）。
2. "正在运行 / 已选中 / 没问题"。

出错用红，不占用蓝。不引入绿色。

## 3. 字体

- 西文和数字：**Archivo**（OFL，woff2 打进安装包 / 站点自托管，不联网加载）。
- 中文：系统字体（苹方 / 微软雅黑 / Noto Sans CJK）。
- 等宽：系统等宽字体，**只给机器值**：哈希、IP、端口、种子、路径。不用来做小标签。
- 标题靠字重（800）和紧字距（-0.03em），不靠装饰。页面标题 28px，面板标题 16px，正文 14px，说明 13px。
- 数字对齐的地方用 `tabular-nums`。

## 4. 形状与结构

- 圆角只有两档：控件 3px，容器 4px。状态点是圆的，**出错的点是方的**（不只靠颜色区分）。
- 面板 = 一圈 1px 的线，底色和页面一样。不用阴影分层；只有对话框和下拉菜单有阴影，并且用 ink 色描边。
- 表格：表头是普通大小写的 13px 灰字，行与行之间一条线。正在运行的行铺 accent-wash。
- 左侧导航的当前项是**黑底白字**（暗色下白底黑字），是整套界面里反差最大的一块，只用来回答"你在哪"。
- 所有页面用同一个 1280px 的外框，标题位置不随导航跳动；表单类内容在框内限宽、靠左。

## 5. 指纹章

`EnvGlyph`：用环境的指纹种子做随机源，画 5 圈断开的同心弧。种子不变图形不变，
复制出来的环境种子不同、章也不同。出现在环境列表、环境详情页标题、官网首屏的示例表里。
这是整套界面里唯一的装饰，也是只有指纹浏览器才会有的东西。

## 6. 文案

- 界面不是文档：常驻文字只留出错原因 + 下一步、需要用户同意的后果、一行输入提示。
- 按钮说清楚点了会发生什么（"新建环境""下载""删除"），同一个动作全程同一个名字。
- 用户看不懂的内部词不出现（状态码只在出错详情里以等宽小字出现）。
- 元信息用中文标点连接（"4 个版本，1 个已下载"），不用"·"。
- 禁用的按钮必须在 `title` 里说明原因。

## 7. 动效

只回应用户的动作（展开、切换、确认）。没有进场动画。尊重 `prefers-reduced-motion`。
